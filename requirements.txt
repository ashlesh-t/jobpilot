apify-client>=1.8
python-dotenv>=1.0
keyring>=25.0
requests>=2.31
python-docx>=1.1
PyPDF2>=3.0
openpyxl>=3.1
# Telegram channel scraper
telethon>=1.36
# URL security pipeline
tldextract>=5.1
httpx>=0.27
python-whois>=0.9
confusable-homoglyphs>=3.0
urlextract>=1.9
# Local control service + UI (server/)
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
apscheduler>=3.10
# Metered engine (Anthropic API). Subscription engine needs the `claude` CLI, not this.
claude-agent-sdk>=0.1
# Optional: tray launcher (python -m server.tray)
pystray>=0.19; extra == "tray"
Pillow>=10.0; extra == "tray"
# Dev/test only:
pytest>=8.0